using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Blog;
using Moq;
using MvcMovie2;
using MvcMovie2.Controllers;
using MvcMovie2.Repository;

namespace Blog.Test
{
    [TestClass]
    public class HomeControllerTest
    {
        //HomeController homeController;

        /*
        [TestInitialize]
        public void Initialize()
        {
            var postRepository = new Mock<IPostRepository>();
            //mock creates method and classes based off the interface
            homeController = new HomeController(postRepository.Object);
        }


        [TestMethod]
        public void Index_When_Returning_HTML()
        {
            var result = homeController.Index();

            Assert.IsTrue(true);
        }
          */
    }
}
